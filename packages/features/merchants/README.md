# merchants

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build merchants` to build the library.

## Running unit tests

Run `nx test merchants` to execute the unit tests via [Jest](https://jestjs.io).
